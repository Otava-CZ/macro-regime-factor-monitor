@page "/trade-ideas"
@inject JournalService JournalService

<PageTitle>Trade Ideas</PageTitle>

<h1>Trade Idea Journal</h1>
<p class="lede">Log discretionary ideas for review. The app intentionally has no broker integration or automatic trading.</p>

<section class="card">
    <h2>New idea</h2>
    <EditForm Model="draft" OnValidSubmit="SaveAsync" FormName="trade-idea-form">
        <DataAnnotationsValidator />
        <div class="form-grid">
            <label>
                Idea date
                <InputDate @bind-Value="draft.IdeaDate" />
            </label>
            <label>
                Title
                <InputText @bind-Value="draft.Title" />
            </label>
            <label>
                Instrument
                <InputText @bind-Value="draft.Instrument" />
            </label>
            <label>
                Status
                <InputSelect @bind-Value="draft.Status">
                    <option>Watching</option>
                    <option>Active</option>
                    <option>Closed</option>
                </InputSelect>
            </label>
            <label>
                Max loss
                <InputText @bind-Value="draft.MaxLoss" placeholder="e.g., 1% NAV or stop level" />
            </label>
            <label>
                Time horizon
                <InputText @bind-Value="draft.TimeHorizon" placeholder="e.g., 2-6 weeks" />
            </label>
            <label class="wide">
                Thesis
                <InputTextArea @bind-Value="draft.Thesis" />
            </label>
            <label class="wide">
                Entry trigger
                <InputTextArea @bind-Value="draft.EntryTrigger" />
            </label>
            <label class="wide">
                Invalidation
                <InputTextArea @bind-Value="draft.Invalidation" />
            </label>
            <label class="wide">
                Catalyst
                <InputTextArea @bind-Value="draft.Catalyst" />
            </label>
            <label class="wide">
                Risk notes
                <InputTextArea @bind-Value="draft.RiskNotes" />
            </label>
            <label class="wide">
                Post-mortem
                <InputTextArea @bind-Value="draft.PostMortem" placeholder="Complete after closure." />
            </label>
        </div>
        <button type="submit">Save idea</button>
    </EditForm>
</section>

<section class="card">
    <h2>Saved ideas</h2>
    @if (ideas is null)
    {
        <p>Loading ideas...</p>
    }
    else if (ideas.Count == 0)
    {
        <p>No trade ideas have been saved yet.</p>
    }
    else
    {
        @foreach (var idea in ideas)
        {
            <article class="journal-entry">
                <div>
                    <strong>@idea.Title</strong>
                    <span class="pill">@idea.Status</span>
                </div>
                <p>@idea.IdeaDate.ToString("yyyy-MM-dd") · @idea.Instrument</p>
                <p>@idea.Thesis</p>
                <dl class="idea-details">
                    <dt>Entry trigger</dt>
                    <dd>@DisplayOrPlaceholder(idea.EntryTrigger)</dd>
                    <dt>Invalidation</dt>
                    <dd>@DisplayOrPlaceholder(idea.Invalidation)</dd>
                    <dt>Catalyst</dt>
                    <dd>@DisplayOrPlaceholder(idea.Catalyst)</dd>
                    <dt>Max loss</dt>
                    <dd>@DisplayOrPlaceholder(idea.MaxLoss)</dd>
                    <dt>Time horizon</dt>
                    <dd>@DisplayOrPlaceholder(idea.TimeHorizon)</dd>
                    <dt>Risk notes</dt>
                    <dd>@DisplayOrPlaceholder(idea.RiskNotes)</dd>
                    <dt>Post-mortem</dt>
                    <dd>@DisplayOrPlaceholder(idea.PostMortem)</dd>
                </dl>
            </article>
        }
    }
</section>

@code {
    private TradeIdea draft = NewDraft();
    private List<TradeIdea>? ideas;

    protected override async Task OnInitializedAsync()
    {
        ideas = await JournalService.GetTradeIdeasAsync();
    }

    private async Task SaveAsync()
    {
        await JournalService.AddTradeIdeaAsync(draft);
        draft = NewDraft();
        ideas = await JournalService.GetTradeIdeasAsync();
    }

    private static string DisplayOrPlaceholder(string? value) =>
        string.IsNullOrWhiteSpace(value) ? "Not specified" : value;

    private static TradeIdea NewDraft() => new()
    {
        IdeaDate = DateOnly.FromDateTime(DateTime.UtcNow.Date),
        Title = string.Empty,
        Status = "Watching"
    };
}
